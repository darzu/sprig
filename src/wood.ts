import { RawMesh } from "./render/mesh.js";

interface Board {}

export function debugBoardSystem(m: RawMesh) {
  const r = getBoardsFromMesh(m);
}

export function getBoardsFromMesh(m: RawMesh) {
  // TODO(@darzu):
  console.log("getBoardsFromMesh");
}
